/*  
  only allows numbers from 0 to 1 
*/
export type Ratio = number

/* 
  orientation of the slider
*/
export type Orientation = 'vertical' | 'horizontal'

export type OneDimensionalSpacePoint = number
